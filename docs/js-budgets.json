{
  "note": "Per-route ceilings on inline <script is:inline> bytes, enforced by frontend/tests/jsbudget.test.ts in `make test-web` (ADR-0046). Raising a ceiling is a decision: do it in the same commit as the growth and say why in BUILDLOG.md.",
  "counted": "Bytes inside <script is:inline> across each route's transitive .astro import graph. Where two page files serve one route, the ceiling is the heavier of the two, since a request reaches one and never both. Comments inside those scripts are counted, because Astro ships inline scripts verbatim and a reader downloads them.",
  "headroom": "Each ceiling is the measured size plus 200 bytes, rounded up to the next 500, so an ordinary comment edit does not fail the build but real growth does.",
  "budgets": {
    "/app/": 21500,
    "/app/404": 21500,
    "/app/429": 21500,
    "/app/about": 21500,
    "/app/classification": 39000,
    "/app/classification/[congress]": 39000,
    "/app/classification/ecct": 21500,
    "/app/data/version-changes": 21500,
    "/app/demo": 21500,
    "/app/design": 43000,
    "/app/diff": 23000,
    "/app/docs": 21500,
    "/app/goto": 21500,
    "/app/guide": 21500,
    "/app/login": 23000,
    "/app/offline": 2000,
    "/app/provisions": 22500,
    "/app/releases": 21500,
    "/app/search": 21500,
    "/app/search/syntax": 21500,
    "/app/settings": 26000,
    "/app/signup": 23000,
    "/app/us/usc": 47000,
    "/app/versions": 21500
  },
  "not_counted": "CopyColumn's <script type=\"application/json\" data-copy-targets>: its size is the section's provision list rather than code, so it varies per URL. Measured once at 4,278 bytes on /app/us/usc/t16/s45f and measured nowhere continuously."
}
